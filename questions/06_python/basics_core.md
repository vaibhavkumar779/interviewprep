# Python - BASICS & CORE
## Questions Only - Test Yourself

### Data Types & Structures
1. What are the basic data types in Python?
2. What is the difference between a list, tuple, set, and dictionary?
3. When would you use a tuple instead of a list?
4. What is list comprehension? Write 3 examples.
5. What is dictionary comprehension?
6. How do you merge two dictionaries?
7. What is the difference between `==` and `is`?
8. What are mutable vs immutable types in Python?
9. What is slicing? Write examples with start, stop, step.
10. How do you sort a list of dictionaries by a specific key?

### Functions
11. What are `*args` and `**kwargs`?
12. What is a lambda function? When would you use it?
13. What is the difference between `return` and `yield`?
14. What is a generator? Why is it memory-efficient?
15. What are built-in functions? Name 10 you use often.
16. What is `map()`, `filter()`, `reduce()`?
17. What is a decorator? Write a simple one.
18. What is a closure?

### Control Flow & Error Handling
19. What is the difference between `for` and `while` loops?
20. What is `try/except/else/finally`? When does `else` execute?
21. How do you raise a custom exception?
22. What is the difference between `break`, `continue`, and `pass`?
23. What is `with` statement (context manager)? Why use it for file I/O?

### String Operations
24. Name 5 string methods you use regularly.
25. What is an f-string? How is it different from `.format()` and `%`?
26. How do you split a string? Join a list into a string?
27. What is regex in Python? What module do you use?
28. Write a regex to match an IP address.
29. Write a regex to match an email address.

### File I/O
30. How do you read a file in Python? Write the code.
31. What is the difference between `read()`, `readline()`, and `readlines()`?
32. How do you write to a file?
33. How do you read and write JSON files?
34. How do you read and write CSV files?
35. How do you handle file encoding issues?

### Interview-Style
36. Write a function to find the most frequent element in a list.
37. Write a function to reverse a string without using built-in reverse.
38. Write a function to check if a string is a palindrome.
39. Write a function to flatten a nested list.
40. Write a script to read a log file and count errors per hour.
